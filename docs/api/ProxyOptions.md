# Type Alias: ProxyOptions

> **ProxyOptions** = `Omit`\<[`Options`](Options.md), `"fatal"`\> & `object`

## Type Declaration

### typeofIsObject?

> `optional` **typeofIsObject?**: `boolean`

Makes the proxy be an object when `true` sometimes needed for react components
