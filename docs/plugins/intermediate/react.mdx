---
sidebar_position: 1
description: Learning to use React in a plugin.
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import CodeBlock from '@theme/CodeBlock';

# React

:::info

This documentation is a placeholder and will be a guide for using React to make plugins.

:::

<Tabs className="code-tabs">
<TabItem value="JS">

```js showLineNumbers
module.exports = props => {
  return React.createElement("div", {className: props.danger ?? "danger-block"},
    React.createElement("p", null, "this is text")
  );
};
```

</TabItem>


<TabItem value="JSX">

```jsx showLineNumbers
module.exports = props => {
  return <div className={props.danger ?? "danger-block"}>
    <p>this is text</p>
  </div>;
};
```

</TabItem>

<TabItem value="CSS">

```css showLineNumbers
div {
  display: flex;
  padding: 5px 10px;
  border: 1px solid #904324;
  background-color: white;
}
 
div.danger-block {
  background-color: #ff0000;
}
 
p {
  text-align: center;
  color: black;
}
```

</TabItem>
</Tabs>

The following is just testing for the highlighter.

```js
module.exports = () => {
  const max = Math.max(45, window.MyValue);
  const action = "Test Them";
  const who = action.split(" ")[1];
  const what = action.indexOf(0, who);

  // Show this to console
  console.log(`Who: ${who} | What: ${what}`);

  var object = {};
  object["do"] = action;
  object[Symbol("self")] = Symbol("self");

  console.log(typeof(object));

  console.log(who + what);
  console.log(10 - 5);
  console.log(2 * 6);
  console.log(9 / 3);
  console.log(5 % 2);

  return class GetTested {

  };
};
```